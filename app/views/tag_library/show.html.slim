div.tags.well.well-sm id="tag-management"
  ul
    - @tags.each do |tag|
      li.tag-cloud id="tag-cloud-li-#{tag.id}"
        span.label.label-fatter
          span.glyphicon.glyphicon-tag
          = tag.tag
          a data-destroy="tag-cloud-li-#{tag.id}" data-remote="true" href="#{user_tag_path(params[:id],tag.id)}" rel="nofollow" data-method="delete"

