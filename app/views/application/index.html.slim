.row
  .owl-carousel.connections-carousel
    - 5.times do |idx|
      .user-profile id="user-profile-#{idx}"
.row
  .owl-carousel.tag-library-carousel
    - @tag_categories.each do |category|
      .tag_library id="tag-library-#{category}" data-category=category